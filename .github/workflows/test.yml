name: Run tests
on: 
  - pull_request
  - merge_group
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with: 
          fetch-depth: 0    
      - run: yarn install --immutable
      - run: yarn test
