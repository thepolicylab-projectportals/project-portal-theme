// this is a .js file so it can be referenced in the gatsby config

const AIRTABLE_TABLE_PROJECTS = "Project Page Content"
const AIRTABLE_TABLE_CONTACTS = "Project Contacts"

module.exports = { AIRTABLE_TABLE_PROJECTS, AIRTABLE_TABLE_CONTACTS }
